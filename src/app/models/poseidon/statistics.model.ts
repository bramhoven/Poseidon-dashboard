export class Statistics {
    public serverCount: number;
    public userCount: number;

    constructor(serverCount: number, userCount: number) {
        this.serverCount = serverCount;
        this.userCount = userCount;
    }
}